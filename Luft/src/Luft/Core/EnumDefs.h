#pragma once
#include <optional>

namespace Luft
{
	enum class FontConfigKey
	{
		font_size_max,
		font_size_title,
		font_size_normal
	};

	enum class ResKey
	{
		font_path_puhui3,
	};

	enum class TextKey
	{
		text_1,
		text_2,
	};
}